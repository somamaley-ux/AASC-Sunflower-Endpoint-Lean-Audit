# Corpus Constraint Source Map

## Dependency Order

1. Finite combinatorics constructs minimal blockers, private residuals, all
   residual/next-blocker incidences, and every source-labelled terminal path.
2. The fixed endpoint activates Admissibility, Standing, Reference, and
   Irreversibility through the mechanized kernel dependency.
3. AASC classifies the standing subrelation of generated reachability.
4. No-standing-sink supplies a standing member only inside an already populated
   finite candidate fibre.
5. Fixed-domain exhaustion proves single-valuedness and excludes a shared
   same-cell standing carrier.
6. The generated injective carrier map composes with the traditional terminal
   blocker embedding into `Fin 4094`.
7. Existing finite-code and link machinery returns to a genuine sunflower and
   the numerical endpoint.

Positive generation therefore precedes governance. Numerical readout follows
carrier typing and collision closure.

## Mechanized Kernel Source

Lake pins
[`somamaley-ux/non-degenerate-construction-kernel-admissibility`](https://github.com/somamaley-ux/non-degenerate-construction-kernel-admissibility)
at commit `8b51f035f86f781e5eeb18cbaef8b46e74ed4924`.
`MechanizedKernelImport` constructs the upstream `ConstructionRegime`, kernel
package, fixed-domain closure, uniqueness, no derivation below the kernel, no
faithful lower generator, and global synthesis for the fixed Sunflower route.

## Relation-Level Corpus Use

The new Lean boundary is `KernelFaithfulStandingPathBridge`. Its fields
correspond to the following corpus machinery:

- standing conservation and no-standing-sink: nonempty standing fibre;
- continuation-locus induction: standing pairs remain within the generated
  endpoint locus;
- anchor/tensor/skin exhaustion: no unlisted same-scope standing branch;
- fixed-domain exhaustion: split, target change, and independent authority are
  the complete non-skin alternatives;
- quotient finality: same complete type cannot sustain two source occupants;
- private-witness finality: source skin-equivalence is literal equality.

The corpus is not used to construct `NextCoordinate`, `IncidencePath`, a
terminal blocker coordinate, `4094`, or a finite enumeration.

## Legacy Population Boundary

`ImportedManuscriptKernelGovernedPopulationTheorem` remains for the previous
endpoint-strength transfer and its exact-strength audit. The generated path
modules isolate an earlier and more concrete handoff. A future direct
internalization should construct `KernelFaithfulStandingPathBridge` without
assuming a support-fibre bound, finite code, carrier injection, or endpoint.

## Claim Discipline

The kernel is a necessity dependency of determinate endpoint reasoning, not an
optional family filter. The current Lean release nevertheless states its
internalization boundary exactly and does not claim an AASC-free conventional
analogue.
