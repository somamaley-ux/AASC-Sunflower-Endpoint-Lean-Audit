# Corpus Constraint Source Map

## Dependency Order

1. Finite combinatorics constructs minimal blockers, private residuals, all
   residual/next-blocker incidences, every source-labelled terminal path, and
   the outgoing-cross/Venn witnesses used by the disposition ledger.
2. Every generated endpoint incidence is already determinate and
   nondegenerate. Admissibility, Standing, Reference, and Irreversibility
   therefore govern it by kernel necessity, not by an optional family filter.
3. Lean restricts comparison to exact coarse cells and proves the local
   private-witness exhaustion: same parent and bounded slot give literal
   identity; every other distinction carries residual-parent tensor content.
4. Raw many-to-one residualization is allowed. Each genuine collision enters
   the exhaustive bounded/separated/sunflower/strict-lower-rank disposition
   before any terminal equivalence is used.
5. Bounded branches are charged, sunflower branches settle, and strict splits
   recurse through a well-founded finite derivation into smaller packages.
6. Only a terminal package that survives those discharges reaches the
   quotient-stable AASC endpoint. Skin cannot create a second standing-bearing
   occupant or alter determinate identity.
7. AASC exhaustion and impossibility close every such terminal package. Lean's
   generic post-exhaustion theorem reconstructs the endpoint up the finite
   derivation tree without raw Hall or a source-to-seed injection.
8. The proved manuscript equivalence transfers that closed AASC endpoint to
   the combinatorial endpoint statement. Equivalence is used after
   disposition; it is not asked to generate the combinatorial witnesses.
9. Exploratory conventional modules record finite seed coding, rank profiles,
   reconstruction overlap, and the `4094` readout for a possible autonomous
   strengthening. They are not conditions on the integrated closure.

Positive generation therefore precedes governance. Structural disposition
precedes equivalence. Numerical reconstruction is a separate downstream track.
Kernel necessity is the source of AASC authority; corpus exhaustion is the
derivation method for its fixed-domain consequences. AASC is invoked late in
the endpoint proof, but it is not activated late.

`GeneratedTerminalKernelGovernance` now proves that the generated terminal
incidence satisfies the kernel dependency automatically: its typed identity is
faithful, its private witness supplies concrete nondegeneracy, the terminal
family remains at the fixed negative endpoint, and all four kernel roles hold.
Kernel applicability is not a residual source obligation.

## Mechanized Kernel Source

Lake pins
[`somamaley-ux/non-degenerate-construction-kernel-admissibility`](https://github.com/somamaley-ux/non-degenerate-construction-kernel-admissibility)
at commit `96978f41a08347f36650d3757cfe0ef79bf864f8`.
`MechanizedKernelImport` constructs the upstream `ConstructionRegime`, kernel
package, fixed-domain closure, uniqueness, no derivation below the kernel, no
faithful lower generator, and global synthesis for the fixed Sunflower route.
This pin exports the generic fixed-role theorem, the current-locus null
exclusion decomposition, and the relational
`CurrentReferenceClassifierSemantics.currentReference_rightUnique` theorem.

The local Lean dependency is intentionally staged.
`kernelNecessityOfNondegenerate` constructs `KernelNecessitySource` from the
determinate non-degenerate endpoint. `concreteFixedDomainClosure` then
exhausts the finite strengthening-factor ledger and constructs
`FixedDomainAPlusClosureSource`. `concreteKernelFirstCorpusMachinery` bundles
them in that order. The second stage is a forced kernel-faithful consequence
calculation, not a family-selecting AASC hypothesis.

## Relation-Level Corpus Use

The standing bridge remains the manuscript-facing vocabulary, but its
inhabitation is now proved equivalent to `ReachableRoleHall`. The
relation-level diagnostic composes the following facts; it is not a claim that
the four unary kernel labels alone imply singleton occupancy:

- fixed endpoint reference is faithful to source identity;
- a shared standing role preserves that fixed reference; therefore
- two distinct sources cannot occupy one role.

`KernelRoleOccupancyAdapter` verifies this implication for the concrete source
reference and the imported construction regime. It deliberately leaves
shared-role reference preservation in the target transport, then proves that
existence of such a transport is equivalent to Hall occupancy. Thus neither
kernel language nor quotient language disguises the exact strength of that
diagnostic route.

`FixedCellStandingClosure` then moves to the manuscript's exact coarse
support/profile/role fibre. `CompleteRoleRefinement` proves local raw exactness
for the residual-parent/slot code and isolates distinct residual parents as the
surviving tensor branch. `OutgoingCrossTower` proves that every dense negative
countercase generates a finite Venn-interference witness.
`PairLocalizedReferenceDivergence` proves that every surviving generated-code
collision has a first merge and an explicit strict lower-rank component split.
`MergeDispositionClosure` corrects the interpretation of that result: the
split is a local reconstruction candidate, while genuine residual merges are
allowed and separately charged. `RankCompatibleProductGate` then distinguishes
unrestricted fullness, graded rank-compatible fullness, and a genuine same-rank
omission. `GradedProductConvolution` proves every fixed-rank slice recursively
bounded and isolates the exact `(r + 2)^k` profile multiplicity. Genuine
same-rank omissions carry injective positive strict binary-rank
reconstructions. `RankWeightedMergeCharging` proves every downstream estimate
from its older, stronger weighted context charge.
`Metricity as Licensed Description` supplies a narrower upstream constraint:
raw cardinality and presentation multiplicity cannot authorize primitive
carrier identity, while quotient-stable comparison, continuation, and
incidence remain tensor content. `QuotientCoverageRigidity` applies that
distinction to the concrete blocker before structural discharge. Any
equivalence under which every original edge-coverage probe descends is equality
on minimal-blocker coordinates, and no proper representative subset remains a
hitting set. This deliberately premature object is named
`UndischargedCoverageCarrierBridge`. Metricity may quotient duplicate paths or
inert record presentations, but coverage-active collisions must first enter
the disposition ledger. The theorem does not constrain the post-exhaustion
equivalence.
`RootAuthenticScopeCorrespondence` then encodes the current manuscript's
root-to-seed relation and proves that generated totality plus terminal
right-uniqueness is exactly `ReachableRoleHall`, and exactly the older
standing-path bridge. This correspondence keeps the dependency order clear:
kernel governance is automatic for the generated determinate nondegenerate
incidence, while one-root-per-terminal-coordinate is an exact Hall-shaped
diagnostic rather than the corrected structural proof mechanism.
`AdversarialCurrentLocusClosure` now separates the two semantic tasks used by
the accepted manuscript: no-standing-sink gives sourcewise occupied generated
loci, and lawful Current-Reference binding gives collision exclusion. The
finite models in `CurrentLocusNecessityAudit` prove that neither task follows
from the unary kernel fields alone. This audits the raw Hall implementation; it
does not test the terminal closeout after bounded, split, sunflower, and
lower-rank branches have been discharged.
`ResidualMergeCounterexample` strengthens that audit using the actual
private-residual tower. The three-edge rank-two triangle is
three-sunflower-free, has two blocker roots, and produces one common terminal
seed. Both colliding incidences remain private-witness nondegenerate at an
automatically kernel-governed endpoint, while Hall and both closure packages
fail. The example is below the dense endpoint threshold, so it does not refute
a dense-case theorem; it proves
that one-root-per-terminal-coordinate cannot follow from kernel governance
alone. The corrected route instead sends the triangle to bounded charge before
terminal transfer.

The rank-profile and binary-reconstruction modules are classified as
non-load-bearing support for a possible autonomous conventional strengthening.
The older `RankUniformMissingGateChargingSource` remains a sufficient
over-approximation for its stated package. None of these sources is a missing
premise of the integrated closure. Hall and
`FixedCellRoleLocusExclusion` remain downstream comparison results rather than
the active merge-resolution mechanism.

`ExplicitReductionPotential.endpoint_of_exhaustion_and_terminalCloseout`
states the controlling route generically: finite generated disposition plus
terminal AASC closeout proves the endpoint. Its terminal-impossibility
specialization makes the exhaustion/impossibility proof mode explicit and does
not use raw source injection.

`TerminalRoleOccupancyCloseout` now expands the terminal side into the exact
constraint sequence: bivalent skin/non-skin exhaustion, skin finality, the
four-role identity ledger, literal branch certification, and certificate
terminality. It also connects the constructed fine-fibre bounded-charge or
strict lower-rank merge disposition to fixed identity. This narrows the
manuscript import boundary from an opaque fixed-identity result.

`KernelNecessityRoleOccupancy` narrows it again. The generated local theorem may
classify a same-identity collision as skin, bounded certificate, tensor split,
sunflower, or attempted independent authority. The separately mechanized kernel
dependency itself removes the fifth branch, without a duplicated occupancy
premise. Canonical endpoint skin supplies quotient
finality, and reduction minimality plus literal certificates supplies terminal
closeout. The typed manuscript theorem consists exactly of the target-specific
five-way classification and its reduction certificates.

The corpus is not used to construct `NextCoordinate`, `IncidencePath`, a
terminal blocker coordinate, `4094`, or a finite enumeration.

## Endpoint Transfer Boundary

`ImportedManuscriptKernelGovernedPopulationTheorem` supplies the manuscript's
endpoint-strength transfer and exact-strength audit. Its nonemptiness is proved
equivalent to the endpoint bound; this is the intended AASC/combinatorics
correspondence, not a failed attempt to derive population conventionally.

The manuscript proves the target-specific five-way witness classification and
reduction certificates at its natural boundary; the typed import joins that
completed theorem to Lean. Kernel necessity, four-role closure, fixed identity,
the support-fibre bound, and the endpoint are derived downstream. A wholly
autonomous conventional replacement would be a distinct stronger proof class,
not a missing stage of this theorem.

## Claim Discipline

The kernel is a necessity dependency of determinate endpoint reasoning, not an
optional family filter. Its fixed-domain AASC mathematics is forced through
kernel-faithful exhaustion, while the target-specific witnesses remain
combinatorial. The current Lean release states that typed interface exactly and
claims the complete integrated theorem, without recasting it as a distinct
autonomous conventional theorem.
