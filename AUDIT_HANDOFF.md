# Audit Handoff

The review order is combinatorics first, then forced structural governance.
The generated blocker paths, collision geometry, finite dispositions,
reduction certificates, and counts are combinatorial. Once their endpoint
carriers are determinate and non-degenerate, kernel necessity already places
them under AASC governance. The later terminal closeout uses forced
exhaustion/impossibility consequences; it does not activate an optional AASC
regime.

## Reviewer Entry

Start with:

1. `SunflowerAASC/V2/GeneratedIncidenceTower.lean`;
2. `SunflowerAASC/V2/ReachableRoleOccupancy.lean`;
3. `SunflowerAASC/V2/KernelRoleOccupancyAdapter.lean`;
4. `SunflowerAASC/V2/FixedCellStandingClosure.lean`;
5. `SunflowerAASC/V2/FixedCellRoleLocusExclusion.lean`;
6. `SunflowerAASC/V2/GeneratedTerminalKernelGovernance.lean`;
7. `SunflowerAASC/V2/CompleteRoleRefinement.lean`;
8. `SunflowerAASC/V2/OutgoingCrossTower.lean`;
9. `SunflowerAASC/V2/ResidualParentExhaustion.lean`;
10. `SunflowerAASC/V2/PairLocalizedReferenceDivergence.lean`;
11. `SunflowerAASC/V2/RootAuthenticScopeCorrespondence.lean`;
12. `SunflowerAASC/V2/CurrentReferenceClassifierAdapter.lean`;
13. `SunflowerAASC/V2/AdversarialCurrentLocusClosure.lean`;
14. `SunflowerAASC/V2/CurrentLocusNecessityAudit.lean`;
15. `SunflowerAASC/V2/ResidualMergeCounterexample.lean`;
16. `SunflowerAASC/V2/ExplicitReductionPotential.lean`;
17. `SunflowerAASC/V2/TerminalRoleOccupancyCloseout.lean`;
18. `SunflowerAASC/V2/KernelReferenceBoundary.lean`;
19. `SunflowerAASC/V2/GeneratedSeedCapacity.lean`;
20. `SunflowerAASC/V2/MechanizedKernelImport.lean`;
21. `SunflowerAASC/V2/KernelNecessityRoleOccupancy.lean`;
22. `SunflowerAASC/V2/ManuscriptKernelTransfer.lean`;
23. `SunflowerAASC/V2/QuotientCoverageRigidity.lean`;
24. `SunflowerAASC/V2/MergeDispositionClosure.lean`;
25. `SunflowerAASC/V2/RankCompatibleProductGate.lean`; and
26. `SunflowerAASC/V2/GradedProductConvolution.lean`.

The first file is the constructive repair. Its main anchors are:

- `nextCoordinate_nonempty`;
- `incidencePath_nonempty`;
- `exists_reachesTerminalCarrier`; and
- `terminalFamily_noSunflower`.

They prove that every initial blocker coordinate has a generated path to a
literal terminal blocker coordinate. This result has no governance,
population, code, fibre-bound, or endpoint premise.

The second file identifies the exact handoff:

- `reachableRoleHall_iff_exists_injective_selector`; and
- `nonempty_standingPathBridge_iff_reachableRoleHall`.

The third file checks the kernel route:

- `sourceReference_injective`;
- `GeneratedRoleReferenceTransport.carrierOf_injective`;
- `GeneratedRoleReferenceTransport.toImportedKernelBridge`; and
- `nonempty_generatedRoleReferenceTransport_iff_reachableRoleHall`.

The fourth file types the manuscript comparison cell precisely:

- `mem_own_fixedComparisonCell`;
- `fixedCellSource_fields_eq`;
- `standingAdmissibleRedescription_sharedCarrier`; and
- `fixedCell_reachableRoleHall_of_standingFiberPopulation`.

The fifth file implements the corrected role-locus closure:

- `CompleteRoleLocusExclusion`;
- `generatedCarrier_injective_of_completeRoleLocusExclusion`;
- `reachableRoleHall_of_completeRoleLocusExclusion`;
- `completeRoleLocusExclusion_of_constraintPopulation`; and
- `fixedCell_reachableRoleHall_of_constraintPopulation`.

It separates positive combinatorial reachability from the AASC collision law.
Standing is downstream of complete role fulfillment, not a separately
populated premise. The complete-signature package proves role-locus exclusion
on every genuine fixed cell and Hall follows.

The sixth file proves that every generated terminal incidence has faithful
typed identity, private-witness nondegeneracy, transported negative-endpoint
membership, and all four necessarily forced kernel roles. Kernel applicability
is automatic and absent from the typed manuscript boundary.

The seventh file closes the local raw-identity question and audits the global
handoff:

- `completeEndpointRole_injective`;
- `distinct_exhausts_into_parentTensor_or_boundedFiberSlot`;
- `sameResidualAndSlot_iff_eq`;
- `sharedTerminalLocusPreservesCompleteRole_iff_exclusion`; and
- `selectedTerminalLocusPreservesCompleteRole_iff_injective`.

Thus no same-parent fibre has unexplained multiplicity. The surviving local
distinction is distinct residual-parent tensor content across ranks.

The eighth file proves that every negative countercase above a comparison
base `base >= k` populates an explicit finite outgoing-cross tower witness.
A completely cross-free tower is bounded by `k^n`. This generated Venn witness
feeds the manuscript's target-specific five-way theorem through the typed
interface.

The ninth file constructs the finite residual profile rather than assuming it.
It combines the generated two-position rank-one seed with the proved `Fin 3`
local fibre slot, embeds the six-position product into the existing 4094-slot
reservoir, and proves that a same-code load must have distinct residual
parents. The forced AASC impossibility consequence then closes any such
collision that the generated route identifies with independent authority. The
file also proves that constructing this route uniformly is equivalent to the
endpoint bound.

The tenth file turns that same-code collision into concrete finite geometry:

- `generatedResidualParentCollision_hasPairMergeWitness` localizes the two
  generated paths to their first common deletion coordinate;
- `exhausts_boundedSlot_or_vennCode_or_sameVennParent` gives the exact
  bounded-slot/different-Venn/same-Venn exhaustion there; and
- `sameCell_distinct_has_rankSplit` proves the surviving same-Venn branch has
  a strict lower-rank inside/outside or occupied-petal decomposition.

The corrected merge layer records the exact theorem scope. The split above is
local to one or two residual parents. A bounded-overlap globalization would
belong to the distinct autonomous conventional strengthening.
`MergeDispositionClosure` permits real many-to-one residualization and proves
the local four-way disposition. The triangle regression is explicitly assigned
to a bounded residual charge.

The global reconstruction files then prove substantially more without using a
terminal selector:

- `ResidualVennPincer` reconstructs every residual injectively from disjoint
  component data and proves exact additive rank;
- `RankCompatibleProductGate` refines the component image into unrestricted
  fullness, graded rank-compatible fullness, or a genuine same-rank omission;
- `GradedProductConvolution` proves every fixed-rank slice recursively bounded
  and gives the explicit `(r + 2)^k * base^(r + 1)` graded estimate;
- every genuine same-rank omission has at most `k` gates, at most one full-rank
  exception per gate, and an injective positive strict binary-rank split for
  every other member; and
- `RankWeightedMergeCharging` proves that a rank-weighted context charge closes
  each gate fibre, the whole missing branch, and the next-rank power estimate.

These modules support a possible autonomous conventional strengthening. The
older `RankUniformMissingGateChargingSource` remains sufficient for its stated
package but over-quantifies over wrong-rank missing tuples. None of this
experimental theorem surface is load-bearing for the integrated proof.

The eleventh file tests the current manuscript's root-authentic scope crossing
at exact strength. Its root-to-seed relation is generated, total, and
right-unique. Lean proves both
`nonempty_rootAuthenticScopeSeedClosure_iff_reachableRoleHall` and
`nonempty_rootAuthenticScopeSeedClosure_iff_standingPathBridge`. Thus the new
language does not hide Hall or injectivity, and its unresolved content is the
target-specific proof of terminal root-reference right-uniqueness.

The twelfth file consumes the new upstream relational kernel theorem. It
derives root-to-seed right-uniqueness from lawful singleton Current-Reference
classifiers and proves that the complete classifier-governed package has
exactly Hall strength.

The thirteenth file compiles the accepted manuscript's adversarial dependency
order. Null exclusion supplies one occupied generated locus per source, and
Current-Reference classifier uniqueness then excludes collisions. Lean also
proves that the complete adversarial package has exactly Hall strength.

The fourteenth file is a finite necessity audit. It proves that the current
unary kernel package can coexist with an all-null generated continuation
system, and that kernel population can coexist with a two-source/one-locus
collision lacking lawful Reference binding. This prevents no-standing-sink or
current-locus binding from being treated as automatic consequences of the
present kernel fields.

The fifteenth file is the concrete residual-merge audit. On the rank-two
triangle it proves no three-petal sunflower, two private blocker roots, one
common residual seed, failure of Hall, noninjectivity of the generated carrier,
and nonexistence of both pre-discharge Hall-shaped packages. The colliding
incidences still satisfy private-witness nondegeneracy and automatic endpoint
kernel governance. This proves that the target-specific dense-cell hypotheses
must exclude or discharge genuine residual merges; generic kernel governance
cannot erase them. It does not test the post-exhaustion endpoint transfer.

The sixteenth file compiles the accepted manuscript's explicit finite reduction
potential, certified split recursion, terminalization theorem, and
minimal-countercase terminality.

`KernelReferenceBoundary.lean` gives a concrete two-action boundary model for
the older unary kernel package. The new relational classifier theorem is the
repaired upstream surface.

`GeneratedSeedCapacity.lean` gives the downstream consequences:

- `KernelFaithfulStandingPathBridge.standingCarrier_reachable`;
- `standingCarrier_choice_independent`;
- `standingCarrierOf_injective`; and
- `cell_card_le_4094`.

`MechanizedKernelImport.lean` pins and instantiates the standalone mechanized
kernel. `KernelNecessityRoleOccupancy.lean` derives the fixed-domain
five-to-four-role closeout from the necessity-rooted packet.
`ManuscriptKernelTransfer.lean` gives the exact manuscript endpoint transfer
and proves the imported population theorem equivalent to the endpoint bound.
That equivalence records the intended correspondence; it is not a refutation
or a conventional generation theorem.

`QuotientCoverageRigidity.lean` isolates the premature source-level quotient as
`UndischargedCoverageCarrierBridge`. Preserving every original coverage probe
before disposition forces source injection. The triangle cannot inhabit that
bridge because its collision is coverage-active, but the collision has already
entered bounded charge before the manuscript's post-exhaustion equivalence is
used.

The merge, product-gate, and convolution files implement the corrected
many-to-one analysis. Their rank-profile and binary-reconstruction results also
support a distinct autonomous conventional strengthening. AASC is not credited
with positive path generation, cross generation, local merge generation, or a
weighted combinatorial charge.

Together these files prove that the Hall bridge and kernel adapter expose their
exact strength. They are retained as diagnostics rather than used to replace
the controlling disposition-and-closeout proof with raw injectivity.

## Post-Exhaustion Endpoint Route

`ExplicitReductionPotential.lean` contains:

- `CertifiedDerivation.settles_of_terminalCloseout`;
- `endpoint_of_exhaustion_and_terminalCloseout`; and
- `endpoint_of_exhaustion_and_terminalImpossibility`.

These theorems evaluate the generated finite disposition tree only after its
bounded, sunflower, split, and lower-rank branches have been resolved. Strict
splits reconstruct from smaller packages; terminal AASC impossibility closes
the surviving quotient-stable leaves. No source-to-seed selector is required.

`TerminalRoleOccupancyCloseout.lean` expands that final step:

- `CertifiedTerminalExit.impossible` reduces every non-skin exit to the
  certificate classes forbidden by terminality;
- `TerminalRoleOccupancyExhaustion.role_injective` derives role uniqueness
  from bivalent skin/non-skin exhaustion rather than assuming injectivity;
- `AASCFourRoleIdentityPopulation.collision` exposes the complete skin,
  bounded-certificate, tensor-split, and sunflower ledger;
- `CertifiedAASCFourRoleBranchRealization.toFixedIdentityRealization` derives
  fixed identity after literal branch certification; and
- `FineFiberFixedIdentitySemantics.identityFinality_of_terminal` specializes
  the result to the constructed bounded-charge/lower-rank merge disposition.

`KernelNecessityRoleOccupancy.lean` roots that closeout upstream:

- `necessityRootedEndpointGovernance` carries only nondegeneracy and the
  mechanized kernel dependency;
- `NecessityRootedEndpointGovernance.noIndependentAuthorizer` derives
  fifth-branch exclusion from the fixed-domain certificate;
- `KernelGovernedGeneratedFourRolePopulation.toAASCFourRoleIdentityPopulation`
  eliminates the generated independent-authorizer candidate;
- `ReductionMinimalFourRoleCertificatePipeline.terminal` derives terminality
  from reduction minimality; and
- `KernelNecessityAnchoredFourRoleWitnessPipeline.dependencyTrace` records the
  complete route through fixed identity.

`ManuscriptKernelTransfer.lean` supplies the manuscript endpoint handoff:

- `KernelImportRoute.admissibilityBivalent`;
- `KernelImportRoute.necessityRootedGovernance`;
- `threePetalNecessityRootedGovernance`;
- `ThreePetalLocalManuscriptPopulation.kernelNecessityTrace`;
- `ThreePetalLocalManuscriptPopulation.fourRoleIdentityClosure`;
- `sunflower_of_importedManuscriptKernelGovernedClosure`;
- `ImportedManuscriptKernelGovernedPopulationTheorem.provesEndpointBound`;
- `ImportedManuscriptKernelGovernedPopulationTheorem.kernelNecessityEndpointTrace`;
  and
- `nonempty_importedManuscriptPopulationTheorem_iff_endpointBound`.

These audit every downstream step and the exact endpoint strength of the
manuscript import. The local imported object now exposes a generated five-way
classification and literal reduction certificates. The manuscript proves that
Sunflower-specific theorem at its natural boundary. The typed import joins it
to Lean, which constructs the kernel-necessity packet, removes independent
authority, derives four-role closure, fixed identity, the support-fibre bound,
and the endpoint.

Mathematically, kernel necessity is the source of AASC authority and
kernel-faithful fixed-domain exhaustion derives its consequence layer. Lean
currently displays these as `KernelNecessitySource` followed by
`FixedDomainAPlusClosureSource` inside `KernelFirstCorpusMachinery`. That
two-stage type is an audit of the derivation, not an optionality claim.

## Validation

```powershell
powershell -ExecutionPolicy Bypass -File scripts\check-sunflower-a-plus-audit.ps1
```

The script scans the published Lean tree, builds the public entrypoint, and
runs the focused axiom checks in
`Checks/Axiom/SunflowerAPlusAudit.lean`.

## Claim Discipline

The strongest supported description is:

> Lean constructs the complete source-by-source blocker-incidence relation and
> literal terminal carriers without AASC. It types the true comparison cells,
> generates outgoing-cross witnesses, allows genuine many-to-one residual
> merges, and exhausts each merge into bounded charge, separation, sunflower,
> or strict lower-rank reconstruction. Lean proves a generic endpoint theorem
> by evaluating this finite disposition tree after terminal AASC
> exhaustion/impossibility forced by kernel necessity; no raw Hall injection
> is used. Lean now expands
> that handoff into a necessity-rooted kernel packet, five-to-four-role
> exhaustion, canonical skin finality, certified branch elimination, and fixed-
> identity finality. The manuscript proves the generated
> witness-and-certificate theorem at its natural boundary, and the typed import
> joins that completed component to Lean. The triangle
> refutes only the premature undischarged
> source bridge and is discharged before transfer. A wholly autonomous
> conventional replacement would be a distinct stronger proof architecture,
> not an unfinished component of this theorem.

Do not describe the path theorem as conditional. Do not describe the
complete-signature exhaustion as already constructed from the countercase. Do
not call the mathematical kernel dependency an optional family predicate. Do
not describe the triangle as refuting the post-exhaustion equivalence, and do
not turn autonomous conventional-strengthening work into a condition on the
integrated closure. Do not describe fixed-domain corpus exhaustion as an
independent source of AASC authority: kernel necessity supplies the authority,
while exhaustion derives its consequences.
