# Critique Regression

This file records the checks that prevent the continuation-generation defect
from returning.

## The Global Tower Is Now Source-by-Source

`GeneratedIncidenceTower.NextCoordinate F source` is the subtype of points in
the next minimal blocker that lie in the current source residual. The theorem
`nextCoordinate_nonempty` proves this type is inhabited because the next
blocker hits every edge of the residual family.

The proof therefore distinguishes three types:

- a source blocker coordinate;
- its residual edge; and
- a next blocker coordinate incident with that edge.

No residual edge is identified with a blocker point.

## The Rank Step Is Defined

The former described map `delta_j` is absent. `IncidencePath` is a dependent
inductive type whose constructor stores the source, literal next blocker
coordinate, incidence proof, and tail path. `incidencePath_nonempty` constructs
a path for every source by induction.

Branching is retained. A representative chosen later has no role in proving
the relation inhabited.

## Exclusion Does Not Manufacture Population

`ReachesTerminalCarrier` and `exists_reachesTerminalCarrier` are proved before
`KernelFaithfulStandingPathBridge` appears. The bridge field
`standingReach_generated` forces every governed pair to carry a proof of this
already generated reachability.

AASC can filter or identify generated candidates; it cannot add one.
This does not make AASC optional. Once the generated carrier is determinate
and non-degenerate, kernel necessity already governs it; only the use of AASC
terminal consequences waits for combinatorial population.

## Terminal Carrier Is Literal

`IncidencePath.terminalCoordinate` is a point of the minimal blocker of
`terminalFamily`. The endpoint bridge no longer receives an abstract
provenance object and assigns it to a proposed seed carrier.

`standingCarrier_reachable` proves that the selected standing carrier is the
terminal coordinate of a generated path.

## Choice Independence Is Separate From Existence

The bridge fields separate:

- standing-fibre nonemptiness;
- standing-fibre single-valuedness; and
- same-carrier source collision closure.

`standingCarrier_choice_independent` uses single-valuedness only after
nonemptiness. `standingCarrierOf_injective` uses same-carrier type equality and
quotient finality only after the carrier has been constructed.

## Bridge Existence Is Exactly Hall Occupancy

`ReachableRoleHall F cell` counts only literal terminal roles already
generated by incidence paths. Lean proves
`nonempty_standingPathBridge_iff_reachableRoleHall` and separately proves Hall
equivalent to a reachable injective selector.

Consequently, the standing bridge cannot conceal a support-fibre bound,
one-occupant-per-token premise, or preselected injection. Any proof that
constructs the bridge has proved the exact no-role-deficit condition.

## Comparison Cells Are No Longer Arbitrary

`FixedCellStandingClosure.fixedComparisonCell` is the exact fibre in which
canonical support, finite constraint profile, and forced AASC role are fixed.
`fixedCellSource_fields_eq` proves that any two sources in this cell agree on
all three fields. Those fields define a fixed coarse comparison cell rather
than a complete AASC type. The complete-role refinement makes that distinction
formal instead of relying on terminology.

## Distinct Local Roles Are Now Exhausted Constructively

`CompleteRoleRefinement.completeEndpointRole_injective` proves that complete
endpoint-incidence role equality is literal private-witness equality. A
distinct pair therefore cannot be skin. The theorem
`distinct_exhausts_into_parentTensor_or_boundedFiberSlot` then gives an exact
two-layer classification: either the rank-lowered residual parent changes, or
the pair shares that parent and occupies different derived `Fin k` slots.

`sameResidualAndSlot_iff_eq` closes the second branch at raw representative
level. Thus one-occupant-per-local-role is proved rather than assumed. Distinct
residual-parent tensor profiles are handled by the generated disposition and
terminal theorem, not by multiplying occupants inside one local role.

## Dense Countercases Now Populate Venn Interference

`OutgoingCrossTower.family_card_le_k_pow_rank_of_crossFreeTower` proves that a
selected-coordinate deletion tower with no outgoing cross at any rank has at
most `k^n` edges. Hence
`outgoingCrossTowerWitness_nonempty_of_card_gt_base_pow` constructs an explicit
finite outgoing-cross tower witness from every sunflower-free countercase above
`base^n` when `base >= k`.

This witness is generated by combinatorics, not asserted by AASC. The
manuscript's completed disposition theorem uses the generated geometry to
classify the distinct residual-parent tensor branch through bounded
certificate, split, sunflower, or certified lower-rank reconstruction.

## Finite Population Is Now Generated Before Impossibility

`ResidualParentExhaustion` constructs a specialized private-residual tower to
rank one. Its terminal seed has two positions, and the already proved local
residual-fibre slot has three. Lean embeds their six-position product into the
existing 4094-position tensor-profile reservoir.

This code is not assumed injective. Instead Lean proves that equality of the
generated code rules out the same-parent branch. A surviving private-witness
load must therefore be a distinct residual-parent collision. The
target-specific combinatorial route first proves that this collision occupies
the attempted independent-authorizer branch. The fixed-domain AASC consequence,
forced by kernel necessity through corpus exhaustion, then eliminates it.

The rank-uniform route is audited by
`nonempty_denseThreePetalGeneratedAuthorizerRouteSource_iff_endpointBound`.
Thus the new finite construction is genuine progress, while the semantic route
cannot be advertised as an independently weaker theorem.

## The Same-Code Collision Is Now Pair-Localized

`PairLocalizedReferenceDivergence` uses equality of the generated rank-one
seed to prove that the two actual recursive deletion paths share a terminal
carrier. `pairMergeWitness_of_sharedRankOneCarrier` then finds their first
merge without assuming an injective selector or Hall.

At that concrete merge, Lean constructs a second matching and an embedded
residual-overlap code. For three petals this code ranges over the eight
elements of `Finset (Fin 3)`. The one-step theorem exhausts the pair into:

1. a bounded local fibre-slot distinction;
2. unequal finite Venn codes; or
3. unequal residual parents with the same Venn code.

The third case is also decomposed constructively. Equal code reflects equal
overlap cell, and unequal parents in one cell differ outside the matching
support or inside a named occupied petal. `sameCell_distinct_has_rankSplit`
proves that the resulting component data has strict lower rank. The former
global collision label has therefore not merely been renamed.

## Root-Authentic Scope Is Audited At Exact Strength

`RootAuthenticScopeCorrespondence` formalizes the current manuscript's
root-to-seed relation directly. It requires every source root to reach a
literal terminal seed and requires one terminal seed not to carry two source
roots. Those clauses construct an injective generated selector.

Lean also proves the converse and therefore the exact equivalences
`nonempty_rootAuthenticScopeSeedClosure_iff_reachableRoleHall` and
`nonempty_rootAuthenticScopeSeedClosure_iff_standingPathBridge`. This defeats a
terminological escape in either direction: the manuscript package is a valid
form of the desired closure, but it is not an independently weaker derivation
of that closure. Automatic kernel applicability remains proved separately;
the no-standing-sink and current-locus Reference-binding consequences do not.

## Hall Is Derived From Role-Locus Exclusion

`CompleteRoleLocusExclusion` states the Pauli-style collision law for a true
fixed AASC cell: two complete-role continuations at one literal terminal locus
are one determinate source. It contains no source assignment, selector, Hall
inequality, finite code, or endpoint bound.

Positive combinatorics already proves that every source reaches a terminal
carrier. Lean combines that total relation with role-locus exclusion in
`reachableRoleHall_of_completeRoleLocusExclusion`. The existing complete
signature package instantiates the law through
`completeRoleLocusExclusion_of_constraintPopulation`. Standing is a consequence
of complete kernel-role fulfillment, not an independently populated grade.

## Seed Capacity Is Downstream

The terminal blocker receives the traditional combinatorial seed embedding.
Only then does `KernelFaithfulStandingPathBridge.seedCode_injective` compose the
maps and `cell_card_le_4094` count the source cell.

Neither `4094` nor an injective code occurs in path generation or carrier
collision closure.

## Kernel Dependency Is Explicit

The Lake manifest pins
`non-degenerate-construction-kernel-admissibility` at commit
`96978f41a08347f36650d3757cfe0ef79bf864f8`.
`MechanizedKernelImport` translates the fixed Sunflower endpoint into the
upstream regime and imports kernel necessity, fixed-domain closure, uniqueness,
no derivation below, and global synthesis. The upstream relational theorem now
derives Current-Reference right-uniqueness from lawful singleton claims and
same-domain classifier uniqueness; it does not assume role injectivity or
right-uniqueness.

The dependency is not two rival authorities. Kernel necessity supplies AASC
authority; the concrete fixed-domain factor exhaustion derives its consequence
layer. Lean exposes those stages separately so that the four unary kernel
labels are not misused as a combinatorial generator or singleton-fibre theorem.

`AdversarialCurrentLocusClosure` now formalizes the paper's intended order:
no-standing-sink first yields sourcewise occupied generated loci, then lawful
Current-Reference classifier uniqueness excludes shared-locus collisions. Its
reverse audit proves the complete package is Hall-equivalent.

`CurrentLocusNecessityAudit` prevents an invalid shortcut. It constructs a
fully kernel-governed two-source regime alongside an all-null generated system,
and a second populated one-locus collision model with no lawful reference
binding. Therefore the currently imported unary kernel package does not itself
prove the two concrete semantic clauses used by the adversarial section.

## The Concrete Residual Tower Can Merge

`ResidualMergeCounterexample` removes any concern that the preceding boundary
models were too abstract. It uses the actual `MinimalBlocker`, private-edge,
`residualFamily`, `terminalFamily`, `cellCarrierOf`, and `ReachableRoleHall`
definitions on the rank-two triangle family. Lean proves that this family is
three-sunflower-free, has two private blocker roots, and sends both roots into
one terminal seed locus. Consequently the generated carrier map is not
injective, Hall fails, and neither the endpoint-role bridge nor the complete
adversarial current-locus package exists. Lean additionally proves that the two
colliding source/locus incidences are private-witness nondegenerate and sit at
the automatically kernel-governed endpoint. Calling the local merge degenerate
therefore does not resolve it.

The corrected regression theorem now goes further:
`triangle_oneStepPairMerge` realizes the actual merge, and
`triangle_merge_has_boundedResidualCharge` proves that it belongs to a literal
residual-fibre charge class of cardinality below three. Thus the example is not
left as an unexplained failure. It validates the intended rule: merges may
occur, but they must be charged or structurally discharged.

The metricity-based quotient proposal is now tested separately rather than
rejected by slogan. `QuotientCoverageRigidity` proves that every relation which
preserves the original blocker coverage tensor is equality on the minimal
blocker: distinct coordinates have distinct private-edge probes. It also proves
that `UndischargedCoverageCarrierBridge`, which applies the quotient before
structural disposition, still induces an injection on distinct coverage-bearing
sources. The triangle cannot inhabit that premature bridge: its collision is
between two source tensors, not two inert path records over one source. It
therefore enters bounded charge before the post-exhaustion equivalence is used.
This regression does not refute that equivalence or reopen the AASC endpoint.
The module also proves
that a proper subset of the minimal blocker cannot remain a hitting set. The
triangle collision is coverage-tensor-active, so it belongs to the bounded-
charge/reconstruction side of the proposed quotient exhaustion, not its skin
side. Metricity remains downstream and non-authoritative, but that fact does
not erase a concrete private-edge incidence tensor.

This does not attack the sunflower conjecture or rule out a dense-case proof;
the triangle does not satisfy the endpoint density hypothesis. It does defeat
the unrestricted semantic inference from determinate kernel-governed
continuation to one-root-per-terminal-coordinate. Dense-cell cross, Venn, and
lower-rank reconstruction data must place the collision in the disposition
ledger before the generic AASC terminal closeout applies.

This is also the exact pattern considered in the accepted manuscript's
“Candidate-collision regression”: two roots with the same singleton terminal
candidate. The manuscript dismisses the second root through standing
propagation and no-standing-sink. The triangle audit proves that this dismissal
does not follow from generatedness, private-witness nondegeneracy, and automatic
kernel governance alone. The dense proof must first show why the additional
countercase constraints forbid or discharge the merge; the post-exhaustion
equivalence is used only afterward.

`KernelRoleOccupancyAdapter` instantiates that theorem with the private-witness
endpoint-incidence reference. Lean then proves that existence of the required
generated reference transport is equivalent to `ReachableRoleHall`, so the
adapter does not rename or relocate the missing population theorem.

The same audit now applies to the root-authentic scope formulation. A full
source-labelled path record preserves its root definitionally; that fact does
not by itself prove that the terminal coordinate is a right-unique root
carrier. The triangle theorem now proves that distinct roots can arrive at one
literal carrier. Their tensor histories must be uniformly reconstructed or
exhausted in the dense case before Current-Reference uniqueness can be invoked.

## Autonomous Conventional Strengthening

`GeneratedTerminalKernelGovernance` closes the dependency antecedent. Every
generated terminal incidence is typed determinately, carries a private-witness
nondegeneracy certificate, remains at the transported endpoint, and receives
all four kernel roles by necessity. No optional kernel-admission predicate is
present.

The development now proves a corrected local merge disposition and a separate
global reconstruction theorem. Every residual is determined by its disjoint
component tuple with exact additive rank. `RankCompatibleProductGate` also
exposes a flaw in treating every absent Cartesian tuple as a useful gate: with
two matching petals, the tuple taking both whole petals is absent simply because
its rank is twice the required rank. The corrected exhaustion distinguishes
unrestricted fullness, graded rank-compatible fullness, and a genuine missing
same-rank tuple.

`GradedProductConvolution` closes all fixed-rank slices and proves the complete
graded branch bounded by `(r + 2)^k * base^(r + 1)`. The displayed factor is the
number of rank profiles, not an unproved kernel application. In the genuine
same-rank missing branch, Lean assigns a nonempty disagreement component and,
apart from one exception per gate, constructs two positive strict lower-rank
pieces whose pair is injective and whose ranks add exactly to the parent.

`RankWeightedMergeCharging` proves the full downstream arithmetic from
the older `RankUniformMissingGateChargingSource`, but that source is now known
to be stronger than necessary because it charges wrong-rank missing tuples too.
These modules are retained as non-load-bearing support for a separately
formulated autonomous conventional strengthening. A local split is not
silently promoted to a global result, and no such strengthening is required by
the integrated theorem.

That is a visible internalization boundary, not a project axiom. The audit
continues to scan for `axiom`, `sorry`, `admit`, and unsafe declarations.

## Terminal Occupancy Is Now Decomposed

`TerminalRoleOccupancyCloseout` removes fixed identity as the primitive local
handoff. It exposes:

- bivalent skin/non-skin status;
- skin finality;
- the complete skin/bounded-certificate/tensor-split/sunflower ledger;
- literal direct-closeout and endpoint-faithful split realization; and
- certificate terminality.

`TerminalRoleOccupancyExhaustion.role_injective` derives role rigidity from
those clauses. `FineFiberFixedIdentitySemantics.identityFinality_of_terminal`
then connects the theorem to the already constructed local merge disposition:
an unresolved same-fibre merge is bounded or strict lower-rank, and either
branch contradicts terminality once its concrete closeout certificate is
supplied. No raw residual map is declared injective.

`KernelNecessityRoleOccupancy` now deepens the dependency one level further.
The generated collision ledger is allowed five candidate outcomes, including
an attempted independent standing authorizer. The four-role ledger is then
derived, not imported: fixed-domain kernel closure removes the fifth branch
through the mechanized dependency certificate rather than a duplicated
occupancy field,
canonical endpoint skin supplies finality, and a reduction-minimal package
excludes the bounded, tensor, and sunflower certificate exits. The audit-facing
`dependencyTrace` records endpoint adequacy, nondegeneracy, kernel roles,
licensed sameness, admissibility bivalence, the fixed-domain interface, no
derivation below the kernel, global synthesis, and the resulting fixed identity.

## Claim Boundary

The mathematical kernel dependency is not an optional regime condition: it is
forced by determinate identity, comparison, admissible transformation,
standing-bearing endpoint use, reference, and finality. The manuscript's AASC
endpoint closure and endpoint equivalence are not reopened by these regression
tests. Lean checks positive generation, the finite disposition machinery, and
the generic theorem that terminal AASC exhaustion/impossibility settles the
endpoint without raw Hall. The manuscript proves the target-specific five-way
witness classification and literal reduction certificates at its natural
boundary; the typed import joins that completed component to Lean. Kernel
necessity removes independent authority; four-role closure, fixed identity,
the support-fibre bound, and the endpoint are derived from that pipeline. The
rank-weighted conventional modules belong to a distinct proof class and do not
condition this endpoint.

Thus the proof remains combinatorics-first in generated content and
kernel-first in structural authority. AASC is a forced consequence layer, not
an optional overlay; the complete positive combinatorial component supplies
the generated content on which that layer acts.
