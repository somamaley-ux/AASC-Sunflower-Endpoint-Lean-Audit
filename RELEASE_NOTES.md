# Release Notes

## Current Dependency Framing

The development is combinatorics-first in generated content and kernel-first
in structural authority. Combinatorics supplies the blocker-incidence paths,
cross/Venn witnesses, collision dispositions, reduction certificates, and
numerical estimates. Every resulting determinate non-degenerate endpoint is
already governed by kernel necessity. Kernel-faithful fixed-domain exhaustion
then derives the AASC consequences used at terminal closeout; it is not an
optional interpretive layer and does not generate the combinatorial input.

## v0.4.0 - Integrated Hybrid Closure

The Lean development now encodes the corrected Pauli-style AASC law:

```text
same complete role + same literal terminal locus -> same determinate source.
```

`CompleteRoleLocusExclusion` is collision-only. Combinatorics independently
proves that every source reaches a terminal carrier, and Lean composes the
total generated relation with role-locus exclusion to obtain an injective
carrier choice and `ReachableRoleHall`. This remains an exact-strength
diagnostic, not the controlling post-exhaustion proof route.

When inhabited, `PrivateWitnessConstraintPopulation` instantiates this law on
each genuine canonical-support/constraint-profile/forced-role cell. Its
complete-signature exhaustion proves the stronger fact that the cell is
subsingleton. This Hall-shaped package is retained as an exact-strength
diagnostic; it is not the controlling post-exhaustion proof route. Standing is
downstream of complete kernel-role fulfillment, not a generated or graded
resource.

`GeneratedTerminalKernelGovernance` proves that the generated terminal
incidence itself satisfies the dependency antecedent. It has faithful typed
identity and private-witness nondegeneracy, remains at the transported negative
endpoint, and receives all four kernel roles by necessity. No optional
`kernelApplies` premise remains.

`CompleteRoleRefinement` now separates local raw exactness from global terminal
transport. Complete endpoint-incidence roles are injective. Every distinct
private witness is either a different residual-parent tensor or a different
derived `Fin k` slot inside one residual parent, and same parent plus same slot
is literal source equality. It also proves that terminal complete-reference
preservation is exactly the role-locus exclusion/injectivity burden.

`OutgoingCrossTower` adds the positive global obstruction. A deletion tower
with no outgoing cross has at most `k^n` edges. Consequently every
sunflower-free countercase above `base^n`, for `base >= k`, inhabits an explicit
inductive outgoing-cross witness retaining the generated residual route to the
cross. AASC supplies none of this population.

`ResidualParentExhaustion` now performs the finite population step before
invoking the AASC terminal closeout. The rank-one terminal seed contributes two
positions and the proved local residual fibre contributes three, giving a
generated six-position code inside the existing `4094` reservoir. Equality of
that generated code rules out the same-parent branch. Only a distinct
residual-parent collision reaches the forced fixed-domain authorizer
impossibility consequence.

`PairLocalizedReferenceDivergence` now opens that collision before the AASC
terminal closeout. Equal generated rank-one seed data forces the two concrete
deletion paths to meet, and Lean extracts a first-merge witness. The merge is
exhausted into a bounded local slot, unequal finite residual Venn codes, or
unequal parents in one Venn cell. In the last case Lean constructs a strict
lower-rank inside/outside or occupied-petal component split. For three petals
the Venn alphabet has eight values.

`RootAuthenticScopeCorrespondence` encodes the manuscript's root-authentic
scope crossing without weakening it. A generated, total, right-unique
root-to-seed relation yields an injective terminal selector. Lean also
reconstructs that package from Hall and proves its inhabitation equivalent both
to `ReachableRoleHall` and to `KernelFaithfulStandingPathBridge`. This confirms
the exact correspondence of those descriptions. It is not the mechanism used
to deny raw merges in the corrected post-exhaustion route.

`AdversarialCurrentLocusClosure` now mechanizes the adversarial section in its
stated dependency order. The imported null-exclusion theorem derives one
occupied generated locus per live standing source, and the imported
Current-Reference classifier theorem then derives right-uniqueness. The
resulting root-to-seed closure yields Hall; the reverse audit proves that the
complete adversarial package has exactly Hall strength.

`CurrentLocusNecessityAudit` checks whether those two semantic clauses follow
from the currently imported unary kernel package. They do not: Lean constructs
an all-null live standing generated system, and separately a populated
two-source/one-locus collision that has no lawful Current-Reference binding.
This shows only that the unary package does not itself generate raw Hall. It
does not test or refute the manuscript's post-exhaustion AASC closeout.

`ResidualMergeCounterexample` now tests the real private-residual construction.
The rank-two triangle is three-sunflower-free and has a two-element minimum
blocker, but both private deletions produce the same one-point residual. Lean
also packages the two colliding incidences and proves that each satisfies the
paper's private-witness nondegeneracy predicate while the endpoint is
automatically kernel-governed. Nevertheless Hall fails, the generated terminal
carrier is noninjective, and neither the endpoint-role bridge nor the complete
adversarial current-locus package exists. The triangle is not a dense endpoint
counterexample, but it proves that terminal source identity is not preserved by
residualization in general. The dense-cell reconstruction hypotheses must
exclude or discharge these merges before the forced AASC collision consequence
is used.

The corrected merge layer now realizes the intended response to that
regression. `triangle_oneStepPairMerge` exposes the literal merge and
`triangle_merge_has_boundedResidualCharge` places it in a fibre of cardinality
below three. `MergeDispositionClosure` proves the general local disposition
without Hall or a selector. `ResidualVennPincer` and
`GlobalMergeReconstruction` then reconstruct every residual from exact disjoint
component data and exhaust the global family into a closed full-product branch
or a missing-gate branch. The latter has at most `k` gates, at most one full-
rank exception per gate, and honest lower-rank context sections.

`RankWeightedMergeCharging` proves the downstream arithmetic after a genuine
weighted deletion charge is supplied: lower-rank induction controls each gate
fibre and one parent-rank unit absorbs the finite gate labels and exceptions.
The newer rank-compatible audit separates wrong-rank omissions from genuine
same-rank compatibility failures. It proves every fixed-rank component slice
sunflower-free, bounds the graded branch by
`(r + 2)^k * base^(r + 1)`, and gives every genuine nonexceptional gate an
injective positive strict binary-rank reconstruction. These modules are
non-load-bearing support for a separately formulated autonomous conventional
strengthening. The older
`RankUniformMissingGateChargingSource` remains sufficient but is stronger than
that separate conventional target. The theorem
`nonempty_denseThreePetalGeneratedAuthorizerRouteSource_iff_endpointBound`
proves the exact endpoint correspondence. That equivalence supports the
transfer; it does not refute the already closed AASC route.

`QuotientCoverageRigidity` now names the premature source-level formulation
`UndischargedCoverageCarrierBridge`. It proves that preserving every original
minimal-blocker coverage probe before disposition would force raw injection.
The triangle cannot inhabit that premature bridge, but it is already assigned
to bounded charge. The regression therefore does not reach the post-exhaustion
equivalence.

`ExplicitReductionPotential` states the corrected architecture directly.
`endpoint_of_exhaustion_and_terminalCloseout` evaluates the finite certified
disposition tree after terminal AASC closeout; strict splits reconstruct from
smaller packages, while bounded and lower-rank branches are discharged before
transfer. Its impossibility specialization closes terminal packages directly,
without Hall or a source-to-seed selector.

`TerminalRoleOccupancyCloseout` expands the final handoff. It proves that
certificate terminality excludes every certified non-skin exit, derives
fixed-role injectivity from bivalent skin/non-skin exhaustion and skin
finality, exposes the complete skin/bounded/tensor/sunflower identity ledger,
and specializes the result to the constructed fine-fibre bounded-charge or
strict lower-rank merge disposition. `KernelNecessityRoleOccupancy` now
constructs the necessity-rooted governance packet, eliminates a fifth attempted
independent-authorizer branch, and derives terminality from reduction
minimality. `ManuscriptKernelTransfer` imports only the target-specific
five-way witness classification and literal reduction certificates; four-role
closure, fixed identity, and the endpoint are derived downstream.
The new endpoint trace records this entire dependency from the minimal
mechanized kernel certificate through the support-fibre bound to the global
three-petal theorem.

Lean records kernel necessity and the concrete fixed-domain consequence source
as two explicit stages of `KernelFirstCorpusMachinery`. This is a mechanized
dependency audit, not a mathematical conditionality claim.

Validation:

- Lean and Mathlib `v4.28.0`;
- 1,143 successful public-entrypoint build jobs;
- 1,146 successful combined entrypoint-and-audit build jobs;
- 250 focused axiom checks;
- no project `axiom`, `sorry`, `admit`, or unsafe escape.

Publication:

- synchronized 48-page integrated hybrid manuscript;
- complete 109-file source package containing all 63 project Lean sources;
- clean Tectonic build with no LaTeX warnings, unresolved references,
  unresolved citations, overfull boxes, or underfull boxes; and
- page-by-page PDFium visual verification.

Release assets:

- `Two_Track_Closure_for_the_Three_Petal_Sunflower_Endpoint_Integrated_Hybrid_Closure_Edition.pdf`
- `Two_Track_Closure_for_the_Three_Petal_Sunflower_Endpoint_Integrated_Hybrid_Closure_Publication_Project.zip`

## v0.3.0 - Generated Blocker-Incidence Paths

This release closes the constructive continuation gap in the earlier
Sunflower proof spine.

For every private blocker coordinate, Lean now defines the next candidates as
the points where its residual edge meets the next minimal blocker. Because the
next blocker hits every residual edge, this subtype is inhabited. Iteration
produces a dependent path for every source and a literal terminal blocker
coordinate for every path. No matching, canonical successor, or AASC
population premise is used.

The historical relation-level bridge is invoked only after this candidate
relation has been generated; kernel governance itself is not activated at that
late point. The bridge records standing reachability as a subrelation of
generated reachability and isolates four governance obligations:
standing-fibre population, single-valuedness, same-carrier complete-type
determination, and quotient finality. Lean proves the resulting carrier is
generated, choice-independent, injective, and bounded by the traditional seed
capacity `4094`.

The synchronized manuscript removes the former undefined rank-step map,
abstract inherited-form tower, terminal seed constructor atlas, and abstract
seed-carrier assignment. Its hostile audit addresses the continuation report
point by point.

The claim boundary is explicit. Lean fully checks positive path generation and
every consequence of `KernelFaithfulStandingPathBridge`; the manuscript proves
the Sunflower-specific terminal theorem at its natural boundary and the typed
interface joins the two completed components. The release is an integrated
combinatorial--AASC theorem, not a claim in the distinct autonomous
single-language proof class.

Validation:

- Lean and Mathlib `v4.28.0`;
- 1,056 successful build jobs;
- 42 focused axiom checks;
- no project `axiom`, `sorry`, `admit`, or unsafe escape;
- 34-page PDF with no LaTeX warnings and all fonts embedded;
- publication ZIP hash verification and clean extracted rebuild.

Release assets:

- `Two_Track_Closure_for_the_Three_Petal_Sunflower_Endpoint_Generated_Incidence_Edition.pdf`
- `Two_Track_Closure_for_the_Three_Petal_Sunflower_Endpoint_Generated_Incidence_Publication_Project.zip`

Validation command:

```powershell
powershell -ExecutionPolicy Bypass -File scripts\check-sunflower-a-plus-audit.ps1
```
